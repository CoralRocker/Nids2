initSidebarItems({"struct":[["SaveInfo","Holds a read object T, and the amount of bytes read for that object. Because Saveable::from_bytes() doesn’t consume the bytes read, this allows the user to tell where in their bytearray they are."]],"trait":[["Saveable","Trait converting from a type to a byte array so that it can be written to a file on the disk. Prefer big-endian endianess for consistency. from_bytes can return a different type than `Self`. That type is `T`. This is used for certain types, such as str and Ref which cannot themselves be created and returned, but which can be saved to bytes."]]});